{
  "name": "phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "build": "rm -rf dist && cd ../../part2/phonebook && pnpm build && cp -r dist ../../part3/phonebook",
    "start": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
